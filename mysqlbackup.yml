---
- hosts: mysqlbackup
  gather_facts: yes
  vars:
     ansible_user: root
     MYSQLUSERS:
         - user: dbbackup
           passwd: monitoring3445
           hosts:
            - 'localhost'
           privs:
            - "*.*:REPLICATION CLIENT,RELOAD"
            - "test%.*:SELECT"
     MYSQL_rootpwd: xdYt2ddd21o90
  roles: 
    - {role: mysqlbackup}
